# Yarn 4 配置
# @deepseek-ai/* 是 dsh 官方插件框架依赖（cordis、cosmokit、schemastery 等）。
# 新版本发布不足 1 天会被 npmMinimalAgeGate 隔离，这里对官方 scope 定向放行；
# 其余包仍受门控。
# 使用传统 node_modules 目录（而非 PnP）：去掉后删 .pnp.cjs / .pnp.loader.mjs 并重跑 yarn install
nodeLinker: node-modules
npmPreapprovedPackages:
  - "@deepseek-ai/*"
